/*
 * Classe que deveria ordenar organização do treeMap por valor da conta
 * Por ter alterado o projeto original, achei irrelevante ter essa classe
 */


/*package bank;

import java.util.Comparator;

public class SortByBalance implements Comparator<String>{

	@Override
	public int compare(Account o1, Account o2) {
		
		int ret = 0;
		
		Account a1 = (Account) o1;
		Account a2 = (Account) o2;
		
		if (a1.getbalance() < a2.getbalance()) ret = 1;
		else if (a1.getbalance() > a2.getbalance()) ret = -1;
		
		return ret;
		
	}

	@Override
	public int compare(String o1, String o2) {
		
		int ret = 0;
		
		Account a1 = (Account) ;
		Account a2 = (Account) o2;
		
		if (a1.getbalance() < a2.getbalance()) ret = 1;
		else if (a1.getbalance() > a2.getbalance()) ret = -1;
		
		return ret;
		
	}

}*/
